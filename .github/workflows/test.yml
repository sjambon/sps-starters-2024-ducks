name: Build and Push Docker Images

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      
jobs:
  build:
    name: 'Build and Push to ACR'
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Docker Login
        uses: azure/docker-login@v1
        with:
          login-server: "spscontainerregistryae.azurecr.io"
          username: "spsContainerRegistryAE"
          password: "B/71bfE3I/cGqLaNiBVXU1+o18brPuRvFH62owuPPt+ACRCwpat2"

      - name: Build and Push to ACR
        uses: docker/build-push-action@v2
        with:
          context: ./backend 
          push: true
          tags: "latest"
          file: ./backend/Dockerfile
          platforms: linux/amd64
